"use client"

/**
 * Footer Bridge
 * 
 * Re-exports the new CondorFooter as `Footer` so all existing 
 * pages that import { Footer } from "@/components/footer" 
 * automatically get the new design.
 */

export { CondorFooter as Footer } from "@/components/condor/footer"
